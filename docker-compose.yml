version: "3.9"

services:
  db:
    image: postgres:16
    container_name: ticketmanager_container
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: ticketmanager
    ports:
      - "5432:5432"
    